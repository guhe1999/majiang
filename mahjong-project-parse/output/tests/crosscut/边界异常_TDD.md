# TDD 测试设计文档：边界异常

## 1. 单元元信息

| 字段 | 内容 |
| :--- | :--- |
| 测试层 | 边界异常 |
| 单元短码 | `DATA` |
| 对应上游产物 | `测试决策蓝图.md` L6、各页面文档 §7 |
| 所属产品 / 模块 | Mahjong Project Parse / 本地数据异常 |
| 覆盖范围 | 文档、索引、页面、脚本、同步 |

## 2. 测试策略

| 优先级 | 测试维度 | 依据来源 | 理由 |
| :--- | :--- | :--- | :--- |
| P0 | 缺索引、空文档、缺文件、缺引用、脚本失败、同步过期 | 蓝图 L6 | 直接影响交付可信度 |
| P1 | 超长文本、无结果、未知状态、重复操作、文档冲突 | 页面文档 §7 | 防止坏数据造成假绿 |

### 2.2 不测什么

- 不测真实麻将业务异常。

## 3. 测试用例清单

| ID | 用例名称 | 优先级 | Given | When | Then | 预期失败原因 | 来源 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| B-DATA-01 | JSON 索引缺失显示错误 | P0 | 索引文件不存在 | 打开页面或运行契约检查 | 显示错误并引导脚本索引 | 缺索引被当成空数据成功 | 蓝图 L6 |
| B-DATA-02 | JSON 字段缺失阻塞契约 | P0 | 索引缺 `status` | 校验索引 | 返回字段缺失错误 | 字段缺失导致页面状态错乱 | 蓝图 L3/L6 |
| B-DATA-03 | 空 Markdown 标记待补充 | P0 | 文档只有标题或空内容 | 执行文档检查 | 标记待补充或阻塞 | 空文档被当成完成 | 蓝图 L6 |
| B-DATA-04 | 缺页面 MD/HTML 暴露阻塞 | P0 | 页面清单声明页面但文件缺失 | 执行契约检查 | 标记缺文件且门禁失败 | 文件缺失未进入门禁 | 蓝图 L3/L6 |
| B-DATA-05 | 源码引用缺失阻止最终完成 | P0 | 核心流程没有 `code-back/mahjong` 路径 | 执行验收检查 | 状态未验证，不可交付 | 引用缺失未影响完成状态 | PRD FR-7 |
| B-DATA-06 | 超长路径不撑破布局 | P1 | 路径长度超过 200 字符 | 渲染路径 | 可换行/滚动且不遮挡内容 | CSS 缺失导致横向溢出 | 蓝图 L6 |
| B-DATA-07 | 搜索无结果有下一步 | P1 | 搜索不存在关键词 | 执行搜索 | 显示无结果和推荐入口 | 空白状态导致用户误判 | 各页面 §7 |
| B-DATA-08 | 未知状态降级待确认 | P0 | 状态值为未知 | 规范化状态 | 输出待确认并进入风险 | 未知状态被当成成功 | 蓝图 L6 |
| B-DATA-09 | 重复复制/快速筛选不乱序 | P1 | 用户快速点击复制和筛选 | 触发连续操作 | 页面状态稳定，反馈不堆叠遮挡 | 事件处理缺陷导致错乱 | 蓝图 L6 |
| B-DATA-10 | 同步过期不能交付 | P0 | `document/` 版本落后 `output/` | 执行同步检查 | 标记过期且阻塞交付 | 过期文档被当成最新 | PRD F5 |

## 4. Mock 策略

| 依赖 | Mock 方式 | 理由 | 关联用例 |
| :--- | :--- | :--- | :--- |
| 文件和索引 | 临时 fixture/虚拟文件树 | 复现坏数据 | 全部 |

## 5. 实现顺序建议

**P0**：`B-DATA-01 → B-DATA-02 → B-DATA-03 → B-DATA-04 → B-DATA-05 → B-DATA-08 → B-DATA-10`

**P1**：`B-DATA-06 → B-DATA-07 → B-DATA-09`
