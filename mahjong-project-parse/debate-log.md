# Debate Log

## 阶段 0 到阶段 1 决策记录

本项目当前为内部项目解析与交付链路，不做外部竞品调研。关键决策来自用户确认和本地项目结构观察。

## 已收敛决策

- 第一版只做解析、文档和契约，不直接修改 `code-back/mahjong` 业务代码。
- 第一版范围为全套解析文档、迁移计划和验收契约。
- 链路产物放在 `mahjong-project-parse/output/`，最终文档同步到 `document/`。
- 成功标准为 lei 能讲清源码结构，he 能判断新需求影响范围。
- 解析深度深入核心流程，包括入口、模块、房间、用户、牌局、RPC、匹配、异常/恢复。
- 验收口径为文档完整性、源码引用、影响范围表和验收契约都齐。
- 脚本边界为轻量扫描/索引脚本，不做复杂自动化。

## 待后续验证

- 只读源码解析后，确认实际模块职责是否与文件名推断一致。
- 解析文档同步到 `document/` 后，确认是否需要长期双写或仅保留最终文档。
- 轻量脚本是否只做扫描，还是需要增加文档完整性检查。
