# Mahjong Project Parse 假设清单

## A-001

- 假设：本次链路目标不是直接重写麻将服务，而是先把 `code-back/mahjong` 解析、文档化，并形成后续可开发/可验收的任务契约。
- 依据等级：B
- 依据：用户明确提出“解析到当前项目目录结构中”，并确认“第一版只做解析/文档/契约，不直接改业务代码”。
- 验证方式：已确认，后续写入 PRD 功能边界。

## A-002

- 假设：`code-back/mahjong` 暂作为原始后端项目保留，不在 PRD 阶段移动或重构。
- 依据等级：B
- 依据：用户确认第一版不直接改业务代码。
- 验证方式：已确认，后续写入 PRD out scope。

## A-003

- 假设：链路产物使用 `mahjong-project-parse/output/`，最终项目文档可同步或引用到 `document/`。
- 依据等级：C
- 依据：用户选择 `2A`。
- 验证方式：已确认，后续写入 PRD 目录约定。

## A-004

- 假设：解析产物的主要读者包括 lei 和 he，其中 lei 更关注源代码理解、设计方案和项目搭建，he 更关注新需求如何推动项目更新。
- 依据等级：B
- 依据：来自 `infi.txt`。
- 验证方式：在 PRD 阶段确认角色与交付物用途。

## A-005

- 假设：第一版解析产物应包含全套解析文档、迁移计划和验收契约。
- 依据等级：A
- 依据：用户选择 `1A`。
- 验证方式：已确认，后续写入 PRD 阶段路线图。

## A-006

- 假设：第一版的业务成功标准不是“代码已实现”，而是“协作者能基于文档理解源码并推动后续需求”。
- 依据等级：A
- 依据：用户选择成功标准 `1A`。
- 验证方式：已确认，后续写入 PRD 成功度量和验收标准。

## A-007

- 假设：解析深度需要覆盖麻将后端核心流程，而不是停留在目录和文件清单级别。
- 依据等级：A
- 依据：用户选择解析深度 `2A`。
- 验证方式：已确认，后续写入 PRD 功能需求。

## A-008

- 假设：第一版验收必须同时检查文档完整性、源码引用、影响范围表和验收契约。
- 依据等级：A
- 依据：用户选择验收口径 `1A`。
- 验证方式：已确认，后续写入 PRD 验收标准。

## A-009

- 假设：第一版脚本只做轻量扫描和索引，不做复杂自动化。
- 依据等级：A
- 依据：用户选择脚本边界 `2A`。
- 验证方式：已确认，后续写入 PRD 功能边界。
